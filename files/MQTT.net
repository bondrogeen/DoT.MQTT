return function (t)
 if t.run and not M then
  print("MQTT")
  M=t
  M.id = string.format("%x",node.chipid()*256):sub(0,6):upper()
  M.topic = "/DOT"..M.id.."/"
  dofile("MQTT.lua")({init=true})
 end 
 return t.run
end
