return function (t)
 if t.run and not _M then
  _M=t
  _M.id = string.format("%x",node.chipid()*256):sub(0,6):upper()
  _M.topic = "/DoT-".._M.id.."/"
  dofile("MQTT.lua")({init=true})
 end 
 return t.run
end
